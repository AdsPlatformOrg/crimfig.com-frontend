# frontend/mobile/chat-app — CrimFig Chat Mobile

**Platform:** iOS + Android
**Framework:** React Native + Expo
**Purpose:** Mobile chat client mirroring `chat.crimfig.com` functionality.

## Setup
```bash
pnpm install && pnpm expo start
```
