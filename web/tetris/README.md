# frontend/web/tetris — CrimFig Tetris

**Subdomain:** `tetrisgame.crimfig.com`
**Framework:** Next.js 15 (App Router) + TypeScript + Canvas/WebGL
**Purpose:** Browser-based Tetris game integrated with CrimFig auth and leaderboards.

## Setup
```bash
pnpm install && pnpm dev
```
