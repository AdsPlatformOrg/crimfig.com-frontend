# frontend/web/reels — CrimFig Reels

**Subdomain:** `reels.crimfig.com`
**Framework:** Next.js 15 (App Router) + TypeScript
**Purpose:** Short video feed — upload, watch, like, comment.

## Setup
```bash
pnpm install && pnpm dev
```
