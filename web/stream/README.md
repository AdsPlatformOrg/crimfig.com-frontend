# frontend/web/stream — CrimFig Stream

**Subdomain:** `stream.crimfig.com`
**Framework:** Next.js 15 (App Router) + TypeScript
**Purpose:** Live streaming hub — broadcast and watch live streams.

## Setup
```bash
pnpm install && pnpm dev
```
